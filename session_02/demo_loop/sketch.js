let cities = ["Warsaw", "Edimburgh", "London", "Detroit"];

function setup() {
    createCanvas(800, 600);

    for(let x = 0; x < cities.length; x++){
        console.log(cities[x]);
    }

}

function draw() {
    background(220);
}
